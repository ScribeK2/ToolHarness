#!/usr/bin/env ruby
require "rubygems"
require "bundler/setup"

# NOTE: --ensure-latest removed intentionally. It failed CI whenever a newer
# Brakeman was published upstream (regardless of our code), coupling our build
# to RubyGems release timing. Dependabot handles the version bump instead.

load Gem.bin_path("brakeman", "brakeman")
