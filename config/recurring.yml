# Recurring jobs for Solid Queue
# Schedule uses cron syntax: "minute hour day month weekday"
#
# Currently no scheduled jobs — add entries here as needed.

production:
  update_check:
    class: UpdateCheckJob
    queue: default
    schedule: every 24 hours
development: {}
